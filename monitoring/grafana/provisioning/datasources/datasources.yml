# =============================================================
# datasources.yml — Grafana datasource provisioning
# Conecta o Grafana ao Prometheus automaticamente.
# =============================================================

apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
