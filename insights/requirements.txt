# Google Gemini SDK — STT/diarização e geração de insights
google-genai>=1.0.0
# HTTP para comunicação com o backend
httpx>=0.27.0
# Processamento numérico (fatiamento de PCM)
numpy>=1.26.0
# Prosódia — pitch (F0), energia, taxa de fala por segmento
librosa>=0.10.2
# Conversão XML (schema Verint) para dict/JSON — xml_raw jsonb fallback
xmltodict>=0.13.0
# Variáveis de ambiente
python-dotenv>=1.0.0
# Testes (parser XML — tests/test_xml_parser.py)
pytest>=8.0.0
# Servidor HTTP interno de embeddings (Fase 25 — base de conhecimento/RAG do
# chat do Call Center) — o backend Java chama POST /internal/embed
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sentence-transformers>=3.0.0
