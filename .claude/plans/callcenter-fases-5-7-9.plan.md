# Plano: fechamento das Fases 5 (resto), 7 (resto) e 9 (resto) do Call Center

**Origem**: pedido do usuário (2026-08-14). **Complexidade agregada**: XG, fatiada em **12 fatias deployáveis independentemente**.
**Migration atual**: `V73__callcenter_cobrowsing.sql` (confirmado com `ls ... | sort -V | tail -1`). **Próxima livre: V74** — reconfirmar antes de cada fatia.

## 0. Escopo

**Entra**: 5d (simulador), 5e.1 (horário/feriados), 5e.2 (transbordo + `transferir_ramal`), 5f.1 (skill), 5f.2 (tela de traço), 7c (blending), 7d (anexos), 7e (Telegram), 9c.1 (agregado fluxo/URA), 9c.2 (agregado chat), 9c.3 (timeline omnicanal + drill-down), 9c.4 (rechamada 24h/7d + top tabulações), 9c.5 (Excel/PDF), 9c.6 (agendamento), 9c.7 (aderência à escala).

**Fica de fora (decisão, não esquecimento)**: WhatsApp; retomada de sessão após reload; WebSocket em tempo real para o cliente (segue polling); `CALLCENTER_CHAT_PUBLIC_QUEUE_ID` com fila real (é operação); nós `agente_ia` (Fase 16), `coletar_entrada` (depende de STT no ARI/Fase 14) e `consultar_api` (SSRF é escopo da Fase 10); **qualquer mascaramento/PII scrubbing** (regra do projeto — Telegram e anexos gravam texto puro, igual ao webchat).

**Efeito nos 7 nós bloqueados**: `horario_funcionamento` e `transferir_ramal` são desbloqueados (5e.1/5e.2); `pausar_gravacao` e `pesquisa_satisfacao` **já estão implementados** (Fases 5c/21 — o plano-mãe está desatualizado nesse ponto, confirmado em `FlowGraphNodeCatalog.IMPLEMENTED_TYPES`); os outros 3 seguem fora.

## 1. Decisões — CONFIRMADAS pelo usuário (2026-08-14)

**D1 — Telegram: bot DEDICADO** (`CALLCENTER_TELEGRAM_BOT_TOKEN`, `CALLCENTER_TELEGRAM_WEBHOOK_SECRET` no `.env`). Nunca reusar o bot de alertas — segredo e ciclo de vida isolados.

**D2 — LONG POLLING, não webhook.** A operação é 100% rede interna, sem entrada da internet. `TelegramLongPollingClient` (loop assíncrono dentro do `backend`, ou processo dedicado — decidir na implementação da 7e conforme o padrão já usado por outros schedulers `@Scheduled`) chama `getUpdates` com `offset` incremental (`cc_telegram_poll_state.last_update_id`, para sobreviver a restart sem reprocessar). Nenhuma rota pública nova é exposta — reduz a superfície de ataque da fatia (o risco "endpoint público novo" do §4 cai do escopo).

**D3 — PDF é requisito real.** Usar **OpenPDF** (fork LGPL/MPL do iText 4 — iText 7 é AGPL e contaminaria a licença do projeto). Tabela simples (cabeçalho + linhas), mesmo teto de 50k linhas do Excel — acima disso, sugerir ao usuário exportar Excel.

**D4 — Configuração de e-mail (SISTEMA → CONFIGURAÇÃO), preparada mas SMTP real fica para depois.**
Nova aba "E-mail" na tela de Configuração do Sistema — mesmo padrão de `SettingsService`/`SettingsController` (chave/valor com `_CREDENTIAL` suffix para senha, nunca exposta em `GET /settings`). Campos: `SMTP_HOST`, `SMTP_PORT`, `SMTP_USERNAME`, `SMTP_PASSWORD_CREDENTIAL`, `SMTP_FROM_ADDRESS`, `SMTP_STARTTLS` (bool), `EMAIL_ENABLED` (bool, default `false`). Adiciona `spring-boot-starter-mail` ao `pom.xml` e um `EmailSenderService` com `JavaMailSender` configurado **dinamicamente a partir do banco** (não `application.properties` estático — mesmo padrão de `ConfigService`, TTL de leitura). Botão "Testar envio" reusando o padrão de `SettingsTestController` (mesmas proteções de SSRF/timeout já usadas nos outros testes de integração). **Nesta leva**: só a tela + serviço + teste de envio. Nenhum fluxo do projeto (agendamento de relatório, alertas) passa a mandar e-mail de verdade ainda — isso fica para quando o usuário configurar o SMTP real e pedir explicitamente a integração ponto a ponto. `cc_report_schedules` (9c.6) grava o canal desejado (`telegram`/`email`) mas o envio por e-mail só dispara se `EMAIL_ENABLED=true` E a config estiver completa; caso contrário, loga aviso e não falha o agendamento.

**D5 — Blending com sobreposição fila→agente.**
- `cc_queues.max_concurrent_chats` (config por fila, tela de fila já existente).
- `cc_agents.max_concurrent_chats` **nullable** (config na tela de agente).
- Regra de resolução em `ChatBlendingService.resolveLimit(agent, queue)`: se `agent.maxConcurrentChats` for `null` OU `0` (**"zerado ou desativado"**, conforme instrução literal), vale o limite da **fila**; se tiver qualquer valor `> 0`, o valor do **agente prevalece** sobre o da fila. "Voz sempre ganha": agente com um canal SIP em estado `EM_ATENDIMENTO`/tocando (`cc_agent_states`) nunca recebe um `claim` de chat novo, independente do limite. Testar os 4 quadrantes (agente nulo/zero × fila com/sem limite; agente com valor > fila; agente em chamada de voz).

**D6 — Anexos bidirecionais, com allowlist de extensão configurável e cota por usuário.**
- Nova tabela `cc_chat_attachment_extensions` (extensão, mimetype esperado, ativo) — tela de configuração "extensão por extensão" (adicionar/remover), reusando o padrão CRUD simples já usado em outras telas de catálogo do Call Center. Upload rejeita qualquer extensão fora da allowlist (checagem por extensão **e** magic-bytes do arquivo — nunca confiar só no nome, mesmo padrão de validação de upload já usado em `InsightsUploadService`).
- Diretório: `/opt/VoipIA/media/chat/anexos/{username}/...` — **por usuário** (quem envia o arquivo, agente ou cliente identificado pela sessão), nome do usuário sanitizado (nunca interpolar direto no path — mesma disciplina de todo path join já usado no projeto).
- Cota configurável por perfil (**"pode ser ajustável"**): default 2 GB por usuário OU 10 dias de retenção a partir da gravação — o que vier primeiro. Configuração fica **na tela de criação de canal de chat** (`cc_chat_channels`, novas colunas `attachment_quota_bytes`/`attachment_retention_days`, default 2147483648/10).
- Scheduler noturno de expurgo (`ChatAttachmentRetentionScheduler`, padrão de `CostAlertScheduler`) remove arquivo com mais de N dias **e** rebaixa quando a cota é excedida (política: nunca aceitar upload que estoura a cota — 413 claro, mensagem indica quanto falta liberar; nunca apagar silenciosamente arquivo dentro da janela de retenção só por estar cheio).
- Cliente do widget público (`callcenter-chat-widget.js`) ganha input de arquivo — sem autenticação de staff, então **AINDA MAIS importante** validar allowlist + magic-bytes + cota no upload do lado do cliente (rate limit já existente da Fase 7b se aplica também a upload).

**D7 — Chave do contato na timeline: telefone normalizado** (`AniNormalizer`, Fase 27) — mesmo gap já aceito, documentado no código como "identidade real fica para a Fase 14".

**D8 — BU nos agregados novos: SEM filtro de BU**, mesmo padrão das 9a/9b já em produção (uniformidade — todos os agregados de call center compartilham o mesmo gap aceito, documentado uma única vez).

**D9 — Ordem confirmada**: 5 → 7 → 9, com 9c.7 (aderência à escala) só depois da 5e.

## 2. Fatiamento

- **5d — Simulador (M, sem migration)**: `SimulatedChannelDriver` + `FlowSimulationService`, reusando `FlowExecutionEngine` **sem modificá-lo**. Nunca persiste `cc_flow_executions`/`_steps`. Nós de IA (`consultar_base`, `pesquisa_satisfacao`) em **modo seco** — teste `never()` provando que nenhum LLM é chamado (senão "Simular" em loop vira custo real). RBAC: reusa `callcenter.fluxos`. UI: painel de roteiro no `FlowEditor.tsx`.
- **5e.1 — Horário/feriados (M, V74)**: **`cc_holidays` já existe (V70) — reusar, não duplicar**. V74 adiciona `cc_business_hours` + `cc_business_hours_slots` + `cc_holidays.calendar_id` nullable (aditivo). `BusinessHoursService.isOpen` com timezone por calendário. Nó `horario_funcionamento` com 3 handles nomeados (`hr-aberto`/`hr-fechado`/`hr-feriado`), padrão da 5c. RBAC: `callcenter.config`. Validação pelo simulador da 5d.
- **5e.2 — Transbordo + `transferir_ramal` (M, V75)** ✅ **implementada (2026-08-14)**: `cc_queues.overflow_queue_id`/`overflow_after_seconds`/`overflow_max_waiting`. `ChannelDriver.transferToExtension` novo. Validação estrita do ramal antes de qualquer concatenação (classe do achado de `AriClient.play`). Detecção de laço A→B→A. **Decisão tomada na implementação**: avaliação do transbordo via polling AMI (`CallCenterQueueOverflowScheduler`, 15s, reusa a infra de Supervisão da Fase 15 — `QueueStatus`/`Redirect`) em vez de editar o dialplan nativo compartilhado `_5XXX` (`app_queue`) — mexer nesse contexto compartilhado por todas as filas foi julgado desproporcional ao escopo desta fatia e mais arriscado, dado que nenhuma chamada real ainda atravessou uma fila. Latência de até 15s no disparo do transbordo é aceita como gap documentado; migrar para overflow nativo do `app_queue` fica para quando houver tráfego real medido.
- **5f.1 — Skill (G, V76)**: criar `CcAgentSkill`/`CcQueueSkill` (tabelas existem desde a V48, sem classe Java). V76 acrescenta `level`/`min_level`. **Tradução honesta: o `app_queue` não conhece skill** — skill vira `penalty`/pertencimento em `AraQueueMember`, documentado no código. Prioridade manual da Fase 12 continua a fonte de verdade.
- **5f.2 — Tela de traço (P)** ✅ **implementada (2026-08-14)**: endpoint já existia (Fase 5b) mas sem filtro de período — corrigido: `list`/`steps` agora exigem `from`/`to` e a busca de passos usa a janela real de `startedAt`/`endedAt` da própria execução (com folga de 15 min), habilitando pruning de partição em vez de varrer todas as partições de `cc_flow_execution_steps` (V72). Tela nova (`ExecutionTracePanel.tsx`) reusa os mesmos componentes de nó do `FlowEditor` (GenericNode/MenuNode/HorarioFuncionamentoNode) em modo somente leitura, destacando nós visitados/arestas seguidas por cima do grafo da versão real usada na execução. Passo sensível nunca teve valor persistido em `detail` pelo motor (`FlowExecutionEngine`) — a tela só decora a UI indicando isso, sem mascaramento novo. **Gap pré-existente encontrado, fora de escopo desta fatia**: `CallCenterDetailReportService` (Fase 9c, relatório "opção escolhida") usa o mesmo método antigo sem filtro de período — mesmo risco de varredura de partição, documentado aqui para revisitar numa fatia futura, não corrigido agora (cirúrgico).
- **7c — Blending (M, V77)** ✅ **implementada (2026-08-14)**: `cc_queues.max_concurrent_chats` + `cc_agents.max_concurrent_chats` (ambos nullable, D5 confirmado — agente nulo/zero usa o limite da fila, qualquer valor `>0` do agente sempre prevalece). `ChatBlendingService.resolveLimit`. "Voz sempre ganha" já garantido estruturalmente pelo guard `AgentState.DISPONIVEL` já existente em `claim()` (Fase 7a) — nenhuma mudança necessária ali. `claim` recusa com 409 claro quando a contagem de chats `active` do agente atinge o limite resolvido. **Gap aceito, documentado**: contagem+assunção estão na mesma transação, mas sem lock pessimista na linha do agente — dois `claim`s simultâneos do MESMO agente em SESSÕES diferentes (duas abas do navegador, no mesmo instante) podem, em tese, ambos ler a contagem antes de qualquer commit e ultrapassar o limite em 1. Risco de baixíssima probabilidade e baixo impacto (limite estourado por no máximo 1 chat), aceito nesta fatia — mesmo padrão de tolerância a corrida rara já aceito em outras partes do projeto na escala atual.
- **7d — Anexos (G, V78)** ✅ **implementada (2026-08-14)**: `cc_chat_attachments` (referencia `cc_chat_sessions`, NUNCA `cc_chat_messages` — particionada desde V71, mesma restrição de FK já documentada na V72) + `cc_chat_attachment_extensions` (allowlist configurável, vazia por padrão) + `cc_chat_channels.attachment_quota_bytes`/`attachment_retention_days` (default 2GB/10 dias). Upload bidirecional (agente via `CallCenterChatController`, cliente via `PublicCallCenterChatController` + widget `callcenter-chat-widget.js`), validado por extensão **e** magic-bytes conhecidos (png/jpg/gif/pdf/zip/docx/xlsx/pptx — extensão fora do mapa pula a checagem, gap documentado), gravado em `/opt/VoipIA/media/chat/anexos/{uploaderKey-sanitizado}/`. `ChatAttachmentRetentionScheduler` expurga por idade (nunca por cota cheia — cota só bloqueia novo upload com 413 claro). `ContentDisposition` (RFC 6266/5987) no download, nunca concatenação crua de filename no header (achado próprio, corrigido antes do commit). Texto/arquivo puro, sem mascaramento.
- **7e — Telegram por long polling (G, V79)** ✅ **implementada (2026-08-14)**: `cc_chat_channels.telegram_bot_token_ref` (token nunca em texto puro na tabela — referência ao .env via `SettingsService`/`EnvFileStore`), `cc_chat_sessions.external_ref` + índice único parcial `(channel_id, external_ref) WHERE closed_at IS NULL`. `TelegramLongPollingClient` — `@Scheduled` chamando `getUpdates(offset)`, `cc_telegram_poll_state.last_update_id` persistido update a update (não só ao fim do lote) para sobreviver a restart sem reprocessar updates já entregues em caso de falha parcial. Sem rota pública nova (D2 — só rede interna). Update → o **mesmo** `CcChatService` usado pelo webchat (`startExternalSession`/`postMessage`). Idempotência por `update_id`. Bot de fluxo funciona sem mudança: 2ª prova da premissa "um flow engine, agnóstico de canal" (a 1ª foi a Fase 24, webchat). **Achado CRITICAL corrigido antes do deploy**: `telegramBotTokenRef` sem allowlist de padrão permitia apontar para qualquer chave do `.env` (ex. `POSTGRES_PASSWORD`) e vazá-la ao Telegram — corrigido com validação em duas camadas (escrita + leitura). Achado HIGH (listener síncrono dentro da transação, I/O bloqueante) e achado MEDIUM (offset só ao fim do lote, duplicando mensagem em falha parcial) também corrigidos. Detalhe completo em `CLAUDE.md`.
- **CFG-email — Configuração de e-mail em SISTEMA → CONFIGURAÇÃO (M, sem migration de callcenter)** ✅ **implementada (2026-08-14)**: nova aba "E-mail (SMTP)" em `Settings.tsx` (`EMAIL_ENABLED`/`SMTP_HOST`/`PORT`/`USERNAME`/`PASSWORD_CREDENTIAL`/`FROM_ADDRESS`/`STARTTLS`), armazenamento via `.env` (mesmo mecanismo de Jira/Zabbix/Telegram/AD — `SettingsService`/`EnvFileStore`, não uma tabela nova). `spring-boot-starter-mail` adicionado ao `pom.xml`. `EmailSenderService` lê o `.env` a cada chamada (via `EnvFileStore.readRaw()`, sem cache/TTL — já satisfaz "nunca application.properties estático" sem precisar de infraestrutura de cache própria), `send()` fail-closed (`isEnabled()`) para o agendamento futuro (9c.6). Botão "Testar envio" em `SettingsTestController.testEmail` — **decisão que diverge do D4 original**: NÃO aplica o bloqueio de IP privado usado em Jira/Zabbix, pelo mesmo motivo já documentado no teste de AD (`testAd`) — um relay SMTP corporativo é quase sempre um host da rede interna por definição, bloquear quebraria o caso de uso real. Validado em produção via curl: timeout de 5s aplicado a host inexistente, 403 sem token.
- **9c.1 — Agregado fluxo/URA (M, V80)** ✅ **implementada (2026-08-14)**: `cc_agg_flow_daily` (volume/desfecho: `COMPLETED`/`TRANSFERRED_QUEUE`/`TRANSFERRED_EXTENSION`/`ABANDONED`/`ERROR`, duração média só das execuções encerradas) + `cc_agg_flow_node_daily` (abandono por nó — conta só o `last_node_id` de execuções mortas, não todo nó por onde a execução passou). `CallCenterFlowAggregationService`/`Scheduler` espelham o padrão exato de 9a/9b; `/reprocess` passa a cobrir fila+agente+fluxo na mesma chamada. Endpoints `GET /callcenter/reports/flows` e `GET /callcenter/reports/flows/{id}/nodes`, RBAC herdado do matcher genérico `/api/v1/callcenter/reports/**` (sem `resource_key` novo). Frontend: aba "Fluxo/URA" nova em `ReportsQueueTab.tsx`, mesmo padrão de seletor+granularidade+tabela das abas Fila/Agente. Suíte do backend 843/843 (as 2 falhas observadas na rodada são os flakes pré-existentes já documentados: timing de `CallCenterDesktopServiceTest` e `ffmpeg` ausente no container Maven ad hoc — ambos não relacionados). `tsc --noEmit`/`npm run build` do `callcenter-platform/frontend` limpos. Deployado (migration V80 confirmada em `flyway_schema_history`) e validado via curl com JWT forjado: 403 sem token, 200 para ADMIN nos 3 endpoints. Release notes `v1.82` registrada.
- **9c.2 — Agregado chat (M, V81)** ✅ **implementada (2026-08-14)**: `cc_agg_chat_daily` (uma por fila/dia) — FRT (só resposta de agente humano, ignora bot), ART (intervalo cliente→próxima resposta, agente ou bot), concorrência média ponderada pelo tempo (sweep-line sobre `started_at`/`closed_at`, sessão ainda aberta conta até o fim do dia) e contenção do bot (só conta canais com `bot_flow` configurado). `cc_chat_messages` (particionada, V71) sempre filtrada por `created_at` com folga de 1 dia sobre a janela do lote de sessões. `/reprocess` passa a cobrir fila+agente+fluxo+chat. Endpoint `GET /callcenter/reports/chats/summary`. Suíte 847/847 (2 flakes pré-existentes). Deployado e validado. Release notes v1.83.
- **9c.3 — Timeline omnicanal + drill-down (G)** ✅ **implementada (2026-08-14)**: `CallCenterTimelineService` — diferente do Perfil do Cliente (Fase 27), a paginação acontece DENTRO do SQL (`LIMIT`/`OFFSET` nativo), não em memória. Como não existe coluna persistida com o ANI normalizado, a normalização (`AniNormalizer`) foi replicada em CTEs SQL puro (`regexp_replace`+`left`+`substring`) — validada manualmente contra a lógica Java com `BEGIN/ROLLBACK` direto em produção (casos: DDI 55, formatação com símbolos, celular sem o 9º dígito, ramal curto) antes do deploy, e confirmada de ponta a ponta com uma linha sintética inserida e revertida na mesma transação. Endpoint `GET /callcenter/reports/timeline?contact=&from=&to=&page=&size=`. **Gap documentado**: sem teste automatizado da query nativa (suíte roda em H2, que não suporta a sintaxe usada — mesmo motivo pelo qual as migrations de particionamento V71/V72 também não têm teste automatizado, só validação manual em produção). Sem UI de "busca por nome" — o campo aceita só telefone (mesma limitação de identidade da Fase 27, sem `resolved_ad_sam`). Release notes v1.84.
- **9c.4 — Rechamada 24h/7d + top tabulações (P)** ✅ **implementada (2026-08-14)**: `CallCenterRecallService.summarize(queueId, from, to)` — rechamada conta o mesmo ANI normalizado em QUALQUER fila da operação (não só a mesma fila, já que transbordo/escolha de URA diferente pode levar o cliente a outra fila numa segunda ligação). Painel novo na aba "Fila (voz)" já existente (sem tela/resource novo), endpoint `GET /callcenter/reports/queues/{id}/recall`. Sem paginação em banco (mesmo gap já aceito no Perfil do Cliente/Fase 27, mesmo volume de dev).
- **9c.5 — Excel/PDF (M)** ✅ **implementada (2026-08-14)**: `CallCenterReportExportService` — decisão revista na implementação: em vez de adicionar OpenPDF (D3 original), reaproveitado **openhtmltopdf** (já dependência do projeto, usado por `AgentReportPdfService`) e **Apache POI** (já usado por `ExcelExportService`) — zero dependência nova. Dois escapes distintos (não compartilhados): `escExcel` só previne injeção de fórmula (=/+/-/@, mesma classe de achado de `ReportCsvBuilder`), `escHtml` escapa entidades HTML (necessário porque o PDF é HTML renderizado). Teto de 50 mil linhas nos dois formatos (413 com mensagem clara acima disso). Endpoints `GET /callcenter/reports/{calls,chats}/export.{xlsx,pdf}`, mesmos filtros dos relatórios já existentes. Validado em produção: Excel com assinatura ZIP (`PK..`) e PDF com assinatura `%PDF` confirmadas via curl.
- **9c.6 — Agendamento (M, V82, depende de CFG-email para o canal e-mail)** ✅ **implementada (2026-08-14)**: `cc_report_schedules` (canal `telegram`|`email`, frequência DAILY/WEEKLY/MONTHLY + hora do dia — não cron completo). `CallCenterReportScheduleScheduler` roda a cada hora cheia, `CallCenterReportScheduleService.runDue` dispara só o agendamento cuja hora bate e que ainda não rodou hoje (`lastRunAt`). Escopo (fila/agente/período) e destinatário congelados na criação, reavaliados só na execução (mesma disciplina fail-closed da Fase 26). Telegram via `TelegramApiClient.sendDocument` (novo, multipart) reusando `CALLCENTER_TELEGRAM_BOT_TOKEN` (Fase 7e); e-mail via `EmailSenderService`, fail-closed (`isEnabled()`) se `EMAIL_ENABLED=false` — loga aviso, marca `lastRunStatus=FAILED`, nunca lança exceção (não derruba o scheduler nem afeta outros agendamentos). CRUD em `/callcenter/reports/schedules`, RBAC herdado do matcher genérico. Suíte 860/860 (as 2 falhas são os flakes pré-existentes documentados). Validado em produção via curl: criar/listar/ativar-desativar/remover, 403 sem token.
- **9c.7 — Aderência à escala (M, V83, depende de 5e)** ✅ **implementada (2026-08-14)**: `cc_agent_schedules` (turno por dia da semana, ISO-8601, deliberadamente separada de `cc_business_hours`/V74 — turno de agente não precisa coincidir com horário da fila). `CallCenterAgentAdherenceService` reusa exatamente o algoritmo de recorte de `CallCenterAgentAggregationService` (Fase 9b), mas a "janela do dia" vira a janela do TURNO escalado; "logado" = qualquer estado exceto OFFLINE. Sem escala cadastrada pro dia da semana → `adherencePct=null` (confirmado via curl: campo omitido no JSON, nunca 0). CRUD em `/callcenter/reports/agent-schedules` + `GET .../{agentId}/adherence`, painel novo na aba "Agente (voz)" já existente. Suíte 862/862 (2 flakes pré-existentes). Fecha a Fase 9 e a leva inteira do plano `callcenter-fases-5-7-9.plan.md`.

## 3. Ordem recomendada

`5d → 5e.1 → 5e.2 → 5f.1 → 5f.2` → `7c → 7d → 7e` → `9c.1 → 9c.2 → 9c.3 → 9c.4 → 9c.5 → 9c.6 → 9c.7`.
5d primeiro sempre (é a única fatia que reduz risco de todas as outras, dado que não há tráfego real de voz). 9c.2 depois de 7c/7e. 9c.7 por último. Releases: 12 = 5d+5e; 13 = 5f; 14 = 7c+7d+7e; 15 = 9c.

## 4. Riscos específicos

Simulador divergindo do motor real (mitigado por não ter lógica de nó própria); **simulação gerando custo de LLM**; **endpoint público novo do webhook** (item mais sensível da leva); token do bot vazando em log; webhook inviável se o servidor for fechado; skill "roteando" sem o Asterisk conhecer skill; skill sobrescrevendo prioridade manual; **consulta varrendo todas as partições** (V71/V72); relatório agendado sem escopo vazando BU; exportação sem teto; injeção de fórmula no Excel; corrida no limite de chats; anexo de cliente não autenticado (fora de escopo); timeline sem identidade real (gap herdado); **nenhuma chamada real de voz jamais atravessou uma fila** (incerteza herdada); escopo crescendo no meio.

## 5. Aceite

**Bloqueadores**: D1–D9 confirmadas.
**Por fatia**: migration reconfirmada por `ls`/`sort -V`; 4 pontos de sincronia de RBAC se houver resource novo; BU aplicada ou gap documentado; toda query em tabela particionada filtra pela coluna de partição; **nenhum mascaramento adicionado**; TDD e suíte 100% verde; `tsc --noEmit` + `npm run build` limpos; revisão paralela sem CRITICAL/HIGH aberto; deploy real com migration confirmada em `flyway_schema_history`; validação por curl com JWT forjado inline; `releases.ts` + docs + `CLAUDE.md` + memória; push `origin main` e `azure main:desenvolvimento`.
**Fecha a leva** quando 5, 7 e 9 estiverem ✅ no §0 do plano-mãe, com os itens descartados registrados como decisão.
