; =============================================================
; asterisk.conf — Configuração geral do Asterisk
; =============================================================

[options]
; Não rodar como daemon (necessário para Docker)
nofork = yes
verbose = 3
debug = 0
; Diretório de gravações de chamadas
astspooldir => /var/spool/asterisk
astlogdir => /var/log/asterisk
