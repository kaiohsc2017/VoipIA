package com.asteriskia.domain.callcenter.cobrowsing;

/** CobrowseRetentionRunResult — resultado de uma execução (agendada ou manual) do expurgo. */
public record CobrowseRetentionRunResult(int purgedCount) {}
